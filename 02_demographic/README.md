# Wrangle & Visualize Demographic Survey Data

These scripts are needed to perform routine quality control operations on the demographic survey data (across full and SPARC groups) and do desired visualizations. **These scripts require that the user has already run the "standardize" scripts and has those scripts' outputs in the proper local folders.**

Check the "purpose" comments in the top of each script for more information on what it does.
